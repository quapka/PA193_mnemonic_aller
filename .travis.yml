language: go
sudo: false

go: 1.13.x
before_install:
    - go get github.com/mattn/goveralls
script:
    - make
    - make build-examples
    - make test
    - $GOPATH/bin/goveralls -service=travis-ci
